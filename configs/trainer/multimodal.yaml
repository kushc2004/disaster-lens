seed: 42
device: auto
epochs: 60
batch_size: 4
num_workers: 2
crop_size: 512
learning_rate: 0.0001
weight_decay: 0.005
warmup_epochs: 3
use_class_weights: true
event_balanced_sampling: true
samples_per_epoch: null
amp: true
checkpoint_dir: outputs/runs/multimodal

